query CartValidationsGenerateRunInput {
  cart {
    lines {
      quantity
      merchandise {
        __typename
        ... on ProductVariant {
          id
          product {
            poolMeta: metafield(namespace: "$app:inventory", key: "pool") {
              value
            }
          }
        }
      }
    }
  }
}
